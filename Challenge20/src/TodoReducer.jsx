export const TodoReducer = (initialState, action) => {
    switch (action.type) {
        case 'ABC':
            throw new Error ('antion not yet implemented')
            brake;
        
        default:
            return initialState;
            
    }
}